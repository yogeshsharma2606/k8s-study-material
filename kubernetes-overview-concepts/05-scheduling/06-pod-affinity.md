# Pod Affinity & Anti-Affinity

Pod Affinity:
Schedule Pods close to other Pods.

Pod Anti-Affinity:
Avoid placing similar Pods together.

Examples:
- Backend close to cache
- Spread API replicas across nodes

Anti-affinity improves availability by reducing single-node failures.
