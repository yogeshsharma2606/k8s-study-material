# Node Affinity

Node Affinity allows Pods to select nodes based on labels.

Modes:
- requiredDuringSchedulingIgnoredDuringExecution
- preferredDuringSchedulingIgnoredDuringExecution

Example:
Schedule only on SSD nodes.

Node Affinity is configured in the Pod specification.
