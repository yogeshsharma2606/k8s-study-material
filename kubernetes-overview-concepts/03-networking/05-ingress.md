# Ingress

Ingress manages external HTTP/HTTPS traffic.

Features:
- Path routing
- Host routing
- TLS termination

Example:

/api -> api-service
/admin -> admin-service

Ingress requires an Ingress Controller.
