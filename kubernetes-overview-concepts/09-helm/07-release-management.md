# Release Management

Every installation creates a Release.

Capabilities:
- Version history
- Upgrade
- Rollback
- Uninstall

Useful commands:
helm history
helm rollback
helm list

Helm stores release metadata inside the Kubernetes cluster.
