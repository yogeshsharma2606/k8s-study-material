# Labels & Selectors

Labels are key-value metadata attached to objects.

Example:
app=web
env=prod

Selectors are used to find matching resources.

Services and Deployments use selectors to identify Pods.

Changing labels can affect traffic routing, so plan them carefully.
